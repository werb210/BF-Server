name: Deploy BF Server

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      # Type errors allowed
      - run: npx tsc --noEmit || true

      # Build allowed to fail silently
      - run: npm run build || true

      - run: echo "Deploy step"
